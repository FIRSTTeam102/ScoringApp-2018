//-
	Page to log in.
	CALLED FROM
		login.js 	'/'
extends ../layout
block content
	form#login-form(name="login-form" method="post" action="")
		h3(class="gear-text-white")=title
		div(class="w3-container w3-padding-16")
			div(class="w3-quarter gear-label gear-inline-padding")
				label Name: 
			div(class="w3-half")
				select(class="w3-select theme-input w3-no-border" type="text" name="username")
					option
					each member, i in members
						option(value=member.name class="w3-bar-item") #{member.name}
		div(class="w3-container w3-padding-16")
			div(class="w3-quarter gear-label gear-inline-padding")
				label Password: 
			div(class="w3-half")
				input(class="w3-input theme-input w3-no-border gear-inline-padding" type="password" name="password")
		div(class="w3-padding-16")
			input(type="submit" class="gear-btn theme-link gear-hover-white w3-btn" value="Login")
	if error == "true"
		script alert(error);